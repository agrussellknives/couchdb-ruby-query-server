#!/usr/bin/env ruby

$:.unshift File.dirname(__FILE__) + "/../lib"

require 'couch_db'
require 'couchdb_view_server'

CouchDB.parse_args ARGV if ARGV.size > 0
CouchDB.loop :view_server if __FILE__ == $0
